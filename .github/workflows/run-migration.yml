name: DB 마이그레이션 실행

on:
  workflow_dispatch:
    inputs:
      revision:
        description: '마이그레이션 대상 (head = 최신, 특정 revision ID 입력 가능)'
        required: false
        default: 'head'

jobs:
  migrate:
    name: Alembic 마이그레이션
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      DATABASE_URL: postgresql+asyncpg://postgres:${{ secrets.FLY_DB_PASSWORD }}@localhost:5432/bodam
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      PYTHONIOENCODING: utf-8
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v4

      - name: Fly.io DB 프록시 터널
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: fly proxy 시작
        run: |
          flyctl proxy 5432 -a bodam-db &
          for i in $(seq 1 30); do
            if nc -z localhost 5432 2>/dev/null; then
              echo "Fly proxy ready (${i}s)"
              break
            fi
            sleep 1
          done
          if ! nc -z localhost 5432 2>/dev/null; then
            echo "ERROR: fly proxy failed to start within 30s"
            exit 1
          fi
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: uv 설치
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Python 의존성 설치
        run: |
          cd backend
          uv sync --frozen

      - name: 장시간 DDL 사전 실행 (fly proxy 타임아웃 우회)
        run: |
          # fly proxy는 장시간 DDL(CREATE INDEX 등) 중 TCP 연결을 끊음.
          # flyctl postgres connect는 직접 연결하므로 타임아웃 없음.
          # IF NOT EXISTS이므로 이미 존재하면 no-op.
          echo "=== 장시간 DDL 사전 실행 ==="
          flyctl postgres connect -a bodam-db -d bodam -c \
            "CREATE INDEX IF NOT EXISTS idx_policy_chunks_embedding_null ON policy_chunks (id) WHERE embedding IS NULL;" \
            2>&1 || echo "WARNING: 사전 DDL 실패 (Alembic에서 재시도)"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: 현재 마이그레이션 상태 확인
        run: |
          cd backend
          PYTHONPATH=. uv run alembic current 2>&1

      - name: 마이그레이션 실행
        run: |
          cd backend
          PYTHONPATH=. uv run alembic upgrade ${{ inputs.revision || 'head' }} 2>&1

      - name: 마이그레이션 결과 요약
        if: always()
        run: |
          echo "## 마이그레이션 실행 결과" >> $GITHUB_STEP_SUMMARY
          echo "- 실행 시각: $(date '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "- 대상 revision: ${{ inputs.revision || 'head' }}" >> $GITHUB_STEP_SUMMARY
